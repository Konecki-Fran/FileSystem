# PostgreSQL configuration for Docker Compose and local API execution.
POSTGRES_DB=f24
POSTGRES_USER=f24
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Optional host port for the full containerized application.
# FRONTEND_PORT=8080
